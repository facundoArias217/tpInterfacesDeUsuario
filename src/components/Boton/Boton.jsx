import {Button} from 'react-bootstrap';

/*const Boton = (props) => {
    return <Button {...props}>{props.texto}</Button>
};*/

const Boton = (props) => {
    return <Button {...props}>{props.texto}</Button>
};

export default Boton;